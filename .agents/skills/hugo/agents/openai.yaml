interface:
  display_name: "Hugo"
  short_description: "Build, edit, validate, and deploy Hugo static sites."
  default_prompt: "Use $hugo to inspect this site, make a scoped content or template change, and validate the Hugo build."
